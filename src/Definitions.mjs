export {Definitions};

let Definitions = {
    GAME_LOOP_INTERVAL: 10, // speed of ticks in ms
    SCORE_INCREMENT_DIV: 500, // how many ticks it takes to increment score
    box: {
        IMG_SRC: './resources/box.png',
        IMG_CURSOR_SCALE: 0.3,
        IMG_OFFSET_X: -125,
        IMG_OFFSET_Y: -125,
    }
};