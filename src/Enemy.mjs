export {Enemy}

class Enemy {
    constructor(type, row) {
    }

    // TODO add enemy to correct cell
    // TODO figure out wave control or some way of spawning enemies (difficulty increase with time)
    // TODO figure out a way to define difficulty with multiplier/multipliers

    update() {

    }
}